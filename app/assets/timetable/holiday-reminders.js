(function(root){"use strict";let cache=null;const load=async()=>cache||(cache=fetch("/assets/data/holidays-cn-2026.json",{credentials:"same-origin"}).then(response=>{if(!response.ok)throw Error("HOLIDAY_DATA_UNAVAILABLE");return response.json()}));const forDate=async date=>{const key=`${date.getFullYear()}-${String(date.getMonth()+1).padStart(2,"0")}-${String(date.getDate()).padStart(2,"0")}`;const data=await load();return(data.days||[]).find(item=>item.date===key)||null};const render=async date=>{const item=await forDate(date);for(const node of document.querySelectorAll("[data-holiday-reminder]")){node.hidden=!item;if(item)node.textContent=item.type==="transfer_workday"?`${item.name} · 学校课程安排可能有调整，请以学校通知为准`: `${item.name} · 假期`}};addEventListener("DOMContentLoaded",()=>render(new Date()).catch(()=>{}));root.AnyClassHolidayReminders=Object.freeze({forDate,load,render})})(globalThis);
